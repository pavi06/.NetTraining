from collections import defaultdict

class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        groupAnagramsDict = defaultdict(list)
        for i in strs:
            key = ''.join(sorted(i))
            groupAnagramsDict[key].append(i)
        return groupAnagramsDict.values()