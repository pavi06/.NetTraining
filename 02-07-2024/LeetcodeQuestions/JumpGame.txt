class Solution:
    def canJump(self, nums: List[int]) -> bool:
        toReach = len(nums)-1
        for i in range(len(nums)-1,-1,-1):
            if(i+nums[i]>=toReach):
                toReach=i
        return True if toReach==0 else False